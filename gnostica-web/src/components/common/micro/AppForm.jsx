import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from "@/components/ui/form";

// Central boundary for react-hook-form primitives. Feature code must import
// these through the Gnostica design system instead of components/ui.
export {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
};

export const AppForm = Form;
export const AppFormControl = FormControl;
export const AppFormDescription = FormDescription;
export const AppFormField = FormField;
export const AppFormItem = FormItem;
export const AppFormLabel = FormLabel;
export const AppFormMessage = FormMessage;
