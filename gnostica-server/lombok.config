# Lombok configuration
# Copy @Lazy to generated constructor parameters so Spring constructor injection
# sees it and can break circular bean dependencies (e.g. PaymentService <-> PayosService).
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
